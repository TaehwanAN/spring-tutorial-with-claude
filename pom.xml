<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<!--  -->
	<name>My Spring Tutorial App</name>
	<!--  -->
	<modelVersion>4.0.0</modelVersion>
    <parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.13</version> <!-- 3.5.x LTS (3.2.4 → 3.5.13 업그레이드) -->
	</parent>
	<groupId>com.demo.myapplication</groupId>
	<artifactId>my-spring-tutorial</artifactId>
	<version>0.1.0</version>
	<!-- artifactId & version: 이 태그들을 기준으로 jar 파일이 생성된다. 예시) target/my-spring-tutorial-0.1.0.jar -->

	<properties>
		<java.version>21</java.version>
		<spring-modulith.version>1.4.10</spring-modulith.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.modulith</groupId>
				<artifactId>spring-modulith-bom</artifactId>
				<version>${spring-modulith.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<!-- spring-boot-devtools -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<optional>true</optional> <!-- 반드시 포함해야함. 개발환경에서만 devtool 사용하게 해줌. -->
		</dependency>
		<dependency> <!-- 어플리케이션 가용성(App Availability) 모니터링을 위한 의존성(Liveliness + Readiness) -->
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<!--
		  OpenTelemetry 방식 분산 추적 + 구조화 로깅
		  ─────────────────────────────────────────────────────────────────
		  micrometer-tracing-bridge-otel
		    : Micrometer Tracing(Spring Boot 표준 계측 API)을 OTel SDK 위에서 동작하도록 연결.
		      HTTP 요청 등 Spring MVC Observation이 발생할 때 traceId / spanId 를
		      자동으로 MDC 에 주입 → 로그 패턴의 %X{traceId}, %X{spanId} 로 출력됨.
		      Spring Boot 3.2.x BOM이 버전을 관리하므로 <version> 태그 불필요.

		  logstash-logback-encoder
		    : 각 로그 이벤트를 JSON 한 줄로 직렬화하는 Logback Encoder.
		      MDC 에 있는 traceId / spanId 를 최상위 JSON 필드로 자동 포함.
		      Elasticsearch(ELK), Loki(PLG), Splunk 등 어떤 백엔드에도 연결 가능.
		      Spring Boot 3.2.x / Logback 1.4.x 와 호환되는 7.4 버전 사용.
		-->
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-tracing-bridge-otel</artifactId>
		</dependency>
		<dependency>
			<groupId>net.logstash.logback</groupId>
			<artifactId>logstash-logback-encoder</artifactId>
			<version>8.0</version> <!-- 9.0은 Jackson 3.0+ 필요 (Spring Boot 4.0 전용) -->
		</dependency>
		<!-- Spring Modulith -->
		<dependency>
			<groupId>org.springframework.modulith</groupId>
			<artifactId>spring-modulith-starter-core</artifactId>
		</dependency>
		<!-- Test Dependencies -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.modulith</groupId>
			<artifactId>spring-modulith-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.tngtech.archunit</groupId>
			<artifactId>archunit-junit5</artifactId>
			<version>1.3.0</version>
			<scope>test</scope>
		</dependency>
	</dependencies>
	
	<!-- build: 실행가능한 jar를 만들기 위한 부분 -->
	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
	
</project>