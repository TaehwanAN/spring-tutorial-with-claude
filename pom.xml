<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<!--  -->
	<name>My Spring Tutorial App</name>
	<!--  -->
	<modelVersion>4.0.0</modelVersion>
    <parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.4</version>
	</parent>
	<groupId>com.demo.myapplication</groupId>
	<artifactId>my-spring-tutorial</artifactId>
	<version>0.0.9</version>
	<!-- artifactId & version: 이 태그들을 기준으로 jar 파일이 생성된다. 예시) target/my-spring-tutorial-0.0.1-SNAPSHOT.jar -->

	<!-- Additional lines to be added here... -->
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<!-- spring-boot-devtools -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<optional>true</optional> <!-- 반드시 포함해야함. 개발환경에서만 devtool 사용하게 해줌. -->
		</dependency>
	</dependencies>
	
	<!-- build: 실행가능한 jar를 만들기 위한 부분 -->
	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
	
</project>